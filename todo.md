# 功能
P1：页面要根据图片的实际尺寸适配每个图片卡片的区域，上下左右都不能留太大的空白。如果是竖图，宽度至少350px，如果是横图，高度至少350px
P1：图片预览样式优化，极简风格，文件名和张数进度统一放到中间。翻页按键也放到中间。
P2：在目录名称的右侧（右对齐），要展示出来其下有多少张图片。最多递归3层。帮助页的统计信息不要了，相关逻辑也一并不要了。
P1：命令增加别名，整理模式下，展示别名，而不是“执行命令X”

# bug

P1：对同一张图片不能进行多次操作，否则第二个操作会报错
