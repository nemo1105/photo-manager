# 功能
P0：页面布局重构，图片保持比例展示，不要裁剪展示。顶部空间占用太多，导致图片无法展示全。目录导航功能，和工作会话操作按钮不要混在一起。文件夹列表使用窄的树形结构展示。层级也能看的很清楚，空间占用还小。右侧就平铺所有图片（如有）。

P1：支持中英文切换，默认根据浏览器语言进行切换。
P1：启动支持一个位置参数，替代工作目录。

# bug

P1：对同一张图片不能进行多次操作，否则第二个操作会报错
P1：设置中，捕获按键状态没有正确展示。点击捕获后，状态要展示成请按按键的明显效果。
